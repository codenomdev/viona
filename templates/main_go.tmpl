package main

import (
	codenomcmd "github.com/codenomdev/viona/cmd"

  // remote plugins
	{{- range .remote_plugins}}
	_ "{{.}}"
	{{- end}}

  // local plugins
	{{- range .local_plugins}}
	_ "viona/{{.}}"
	{{- end}}
)
func main() {
	codenomcmd.Execute()
}